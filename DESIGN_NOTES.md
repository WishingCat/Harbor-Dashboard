# Harbor 界面精简记录

## API 复检与接入指南（2026-09-16）

侧边栏底部新增独立「API 接口」页（`?project=PROJECT_ID&view=api`），展示当前项目和网站地址、项目 Token 获取流程、Python / curl 示例、任务与已有 Rollout 上传、追加产物及质检查询。认证、追加与接口参考按需展开；代码支持复制，在剪贴板不可用时选中代码供手动复制。切换项目时保留指南页并更新示例，空项目提供创建任务集步骤。

复检修复三类后端边界问题：布尔 Reward 不再误判结果，超大整数不会导致导入失败；损坏 ZIP 返回 400 并回滚；非法翻译地址及端口返回 422，保留已有配置。网页目录上传在超过 1,000 个文件时提示使用 ZIP，与服务端限制一致；ZIP 展开上限仍为 2,000 个文件。示例在认证或创建任务集失败后中止，上传前校验项目与任务集，防止沿用旧 ID 或回退默认集合。

验证：145 项后端及客户端测试、15 个子测试通过；隔离真实 HTTP 29 项检查及修复后 6 项检查通过。覆盖登录、项目令牌、任务与产物上传、重试去重、评审查询、项目隔离和令牌撤销，记录见 `output/api-recheck-20260916-074658/summary.json`。实际 DeepSeek 网关完成一次临时任务文档的中译英流式翻译；未向正式项目提交测试任务、结果或评审。主库完整性检查通过，仍为 133 个用户、2 个项目、1 个任务集、2 个任务。

前端生产构建、依赖兼容性检查通过；npm audit 未报告已知漏洞。浏览器验证桌面及 390px 手机明暗主题、代码和表格滚动、复制与方式切换、空项目说明、项目切换、深链接刷新、生产构建页面和交互式 API 文档，页面无横向溢出、控制台无 error。

指南另完成 27 项验证：从实际页面组件生成 18 个代码块，分别通过 bash/zsh 语法检查，并实际执行 Python/curl 的认证、新建任务集、上传、追加和查询。错误凭据会清除旧 Token；新建任务集失败时不会上传到默认集合。详情见 `output/api-recheck-20260916-074658/guide-examples/summary.json`。

## 项目、任务集与暗黑模式（2026-09-16）

内容层级改为「项目 → 任务集 → Harbor 任务」。全局导航保留项目选择并增加新建项目入口；项目任务库使用紧凑的任务集列表，进入任务集后显示任务和上传入口。项目与任务集创建均只需名称，所有登录成员可操作。上传目标在弹窗中只读显示，任务仍由文件名自动命名。

顶栏增加主题切换按钮，首次跟随系统，用户选择后保存到本机并在其他标签页同步。初始化脚本在页面绘制前应用主题；暗色使用深绿中性色、浅色文字和淡绿操作色，覆盖目录、Markdown、代码、结果、评审、表单和设置，不改变上传图片颜色。保持原有桌面常驻侧栏与手机抽屉规则。

验证：137 项后端及客户端测试、15 个子测试通过，TypeScript 与生产构建通过。隔离普通成员账号实际完成网页新建项目/任务集，CLI 新建第二任务集并上传任务与已有结果；跨集合访问被拒绝，重复上传复用结果。浏览器验证名称校验、三级导航、旧链接补全、跨项目后退、深浅切换与刷新记忆，以及桌面/390px 的列表、文档、源码、双语、评审和上传弹窗。深色关键文字配色对比度均大于 4.5:1，页面无横向溢出，控制台无 error。未调用翻译或向正式项目提交测试内容。

主数据库迁移前备份位于 output/backups/before-task-sets-20260916-150338/；逐表对比确认原账号、任务、文件记录和其他关联数据保留。ProjectAA 的两道临时任务归入默认任务集，PaperBenchX 仍为空。

## 随页面切换的常驻侧栏（2026-09-16）

按用户给出的两张参考图调整导航：桌面任务库、结果和设置页常驻 240px 全局导航；进入任务后隐藏该导航，左侧改为 264px 文件目录。目录从顶栏下方延伸到窗口底部，独立滚动；选文件后继续保留。返回任务库或使用浏览器返回时自动恢复项目导航。

沿用现有字体、森林绿 `#235347`、文字 `#1f2d26` / `#46584d` 和控件样式，以保留式改版处理；`DESIGN_VARIANCE=4`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=2`。仅应用 design-taste-frontend 的现状审查与保留原则，营销页的 Hero、图像和 SEO 模块不适用于本次产品导航调整。

1100px 及以下继续使用抽屉，手机选文件后收起目录并回到正文。跨断点清理抽屉状态与滚动锁，保留可用焦点落点。评审按需展开，桌面常驻目录保持可用；双语在正文空间不足时上下排列。

TypeScript 与生产构建通过。浏览器已验证桌面与 390px 手机、1100 / 1101px 边界、项目切换、进入任务与浏览器返回、文件切换、评审和双语组合、帮助弹窗及滚动锁清理。导航跨断点后的焦点恢复在 DOM 更新完成时处理。正文和侧栏无重叠、页面无横向溢出，控制台无 error；未提交评审或修改工作区数据。

## Agent API 接入（2026-09-15）

Agent API 最初通过工作区设置管理令牌；该界面区域已于 2026-09-16 移除。后端上传 API 与零依赖 Python 客户端保留，令牌改用登录会话调用 HTTP 接口管理，步骤见 [Agent API 文档](docs/AGENT_API.md#1-获取项目-token)。

HTTP API 支持上传本地任务及已有结果、追加结果和查询质检意见，返回可直接打开的任务链接。令牌仅保存哈希并限定项目；相同上传重试复用结果，失败时回滚归档与数据库。客户端不执行上传材料。

验证：121 项测试及 11 个子测试通过，TypeScript 与生产构建通过。隔离 HTTP 环境实际验证下载客户端、目录与已有 trial 一起上传、追加结果、重复请求、跨项目拒绝、评审状态更新与令牌撤销。浏览器验证令牌创建、一次性显示、刷新、撤销、390px 手机布局及上传返回链接。测试令牌均已撤销，测试服务已关闭。当前本地服务已更新，主工作区仍为 133 个用户、ProjectAA 两道临时任务、PaperBenchX 空项目；对外地址保留可配置，等待用户提供服务器后部署。

## 悬浮目录与随包结果（2026-09-15）

这轮按用户修正，目录入口固定悬浮在左下角；桌面目录以浮层覆盖页面边缘，展开前后正文宽度不变。手机仍使用可关闭的目录抽屉，选文件后回到正文；保留 Escape、键盘焦点和滚动锁清理。

任务详情移除独立上传 Rollout 的操作。只有任务包确实包含已识别的 Rollout 结果，才显示结果页签、trial 选择和对应文件；项目无结果时隐藏结果导航、统计和表格列。帮助文案改为上传任务时一并携带已有结果。

上传改为单 ZIP 或任务文件夹，加一个可选简介。名称由 ZIP 文件名或文件夹名生成，以只读文本预览；分类、难度和标签由后端从清单读取或采用默认值。没有填写简介时保留空值。

验证：生产构建通过；85 项后端测试通过，包含命名、可选字段、ZIP 安全、文件保全、结果识别、项目隔离和不执行上传脚本。通过隔离 HTTP 上传接口分别导入纯任务包、含两个 trial 的任务包，再在浏览器核对自动名称、结果页签、两份 Reward、报告和相对文件链接。桌面与 390px 手机检查通过，正文在目录展开前后均为1497px（桌面视口1512px）；手机上传表单仅一个非文件输入项，简介不必填，无横向溢出。浏览器扩展限制了自动本地文件选择，因此该步骤未完成浏览器自动化验证，文件导入由隔离 API 实际验证。测试数据仅位于 output/reader-import-qa/，未加入用户项目。


## 阅读优先改版（2026-09-15）

审查时，详情页同时常驻约 218px 工作区导航、210px 任务目录和290px协作评审；约1500px屏幕只剩中间一半宽度给文档。顶栏、标题、简介、作者标签和工具栏叠加，正文约在首屏中部开始，文档又有独立滚动区域。

这轮保留森林绿 `#235347`、文字 `#1f2d26`/`#46584d`、现有字体与控件圆角，采用 `DESIGN_VARIANCE=4`、`MOTION_INTENSITY=2`、`VISUAL_DENSITY=2`。按用户要求调整阅读布局：全局导航改按需抽屉，正文默认独占页面；目录和评审默认关闭、互斥展开；标题信息逐步展开。使用现有 React、原生 CSS、Lucide；无需营销图像、Hero 或新设计系统。

全局导航采用原生 dialog 管理背景焦点，Escape、遮罩和关闭按钮均可收起；导航到其他项目或页面后自动收起。顶栏固定56px，浏览器前后退同步收起导航；工作区登录、项目入口和页面标签保留。

层级规则：阅读工具栏和全局顶栏20、辅助面板遮罩70、窄屏辅助面板80、现有弹窗100；全局导航使用浏览器原生顶层。动画仅用于展开时的轻微位移，尊重 reduced-motion。

本轮验证：

- TypeScript 和 Vite 生产构建通过；浏览器控制台无 error。
- 在约 1500px 桌面、900 × 1000、390 × 844、320 × 800 检查阅读与面板布局。320px 下上传使用带可访问名称的图标入口，工具栏保持两行；页面无横向溢出。
- 默认目录和评审均关闭，选文件后收起目录并聚焦正文；源文件/预览切换、下载入口、Rollouts 空状态和任务信息展开正常。
- 桌面双语并排、平板及手机上下排列；这轮只检查面板交互，未调用翻译服务。
- 未提交的评审文字与选项在关闭、切到目录、重新打开后保留；验证草稿已清理。检查 Escape、Tab/Shift+Tab 循环、关闭后的焦点恢复。
- 从桌面切到手机时叠加全局导航和评审，再逐层关闭，滚动锁正确释放；评审输入框跨断点保持焦点。
- 验证 PaperBenchX 项目切换后自动收起导航、浏览器返回恢复 ProjectAA 任务、搜索快捷键、导航转帮助弹窗与退出弹窗。临时视口覆盖已恢复。

## 设计判断

面向 Harbor 任务作者和评审者的轻量工作台。按用户要求减少重复说明，让任务、文档、状态与操作成为页面重点。

采用 design-taste-frontend 的保留式改版流程：审视当前页面后删减冗余模块，保留森林绿品牌、页面导航、任务链接与业务功能。参数为 DESIGN_VARIANCE = 4、MOTION_INTENSITY = 2、VISUAL_DENSITY = 3。继续使用已有 React、原生 CSS 和图标库。

## 本次调整

- 任务列表改为通栏，移除重复的最近动态侧栏、评审小贴士、翻译介绍和侧栏宣传文案。
- 删除统计说明、英文装饰小标题、标题下重复介绍与装饰页脚。统计仅保留名称和数字。
- 搜索集中到列表工具栏，保留 Command / Control + K 聚焦搜索；删去重复的搜索输入框。
- 精简任务详情、翻译初始面板、评审空状态、设置及登录和上传弹窗。实际任务说明、文件内容、评论、字段要求和错误反馈保留。
- 主文字改为 #1f2d26，次要文字改为 #46584d；修复示例标签、动态正文、占位文字、禁用按钮与控件图标的旧浅色。
- 手机统计压为单行，任务表保留任务和评审状态；平板双语阅读上下排列，评审消息与表单并列。

## 验证

- TypeScript 与 Vite 生产构建通过。
- 在 1440 × 1000、900 × 1000 和 390 × 844 下检查布局。任务列表、详情与平板阅读无页面横向溢出。
- 浏览器验证搜索、状态筛选、搜索快捷键、详情打开、翻译面板开关、登录弹窗、上传空文件错误提示。
- 读取实际渲染文字和背景色检查对比度，覆盖任务库、详情、翻译、运行记录、团队动态、设置、登录与上传表单。检查到的最低比值为 5.48:1；这属于文字对比度专项检查，不是完整 WCAG 审计。
- 上传表单以浏览器请求模拟的成员身份检查，未创建账号、任务或调用翻译接口。
- 截图保存在 output/playwright/clean-*.png。

## 适用范围

工作台沿用浅色主题、森林绿操作色和原有圆角规则，保留清晰焦点及 reduced-motion 支持。展示网站的 hero、营销图片、跑马灯和 SEO 迁移检查不适用于此次工作台精简。用户上传的文档内容不受营销文案规则约束。

## 项目任务库

侧栏在字标下提供 ProjectAA / PaperBenchX 项目选择器，当前项目也显示在面包屑中。任务、计数、动态、运行记录与上传均限定到所选项目；弹窗只读显示目标项目，分享链接携带项目 ID。项目切换清空搜索与筛选状态，旧请求完成后不会覆盖新项目的数据。保留现有简洁列表，不增加项目介绍卡片或装饰说明。

默认 ProjectAA 提供两道临时任务，PaperBenchX 为空。项目中的任务可以持续追加，平台翻译配置仍由后端统一管理。

项目改动验证：后端 46 项测试通过；真实旧数据副本迁移后为 2 / 0，主工作区也已验证同样结果。浏览器检查了快速切换期间延迟响应、前进后退、刷新、旧任务链接与文件下载归属。两项目上传表单通过拦截提交检查 project_id，无测试任务写入主工作区；桌面和手机截图见 output/playwright/projects-*.png。
